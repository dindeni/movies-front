import styled from 'styled-components';

const StyledWrapper = styled.div`
  position: relative;
  padding-top: 50%;
`;

const StyledIframe = styled.iframe`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
`;

export { StyledWrapper, StyledIframe };
